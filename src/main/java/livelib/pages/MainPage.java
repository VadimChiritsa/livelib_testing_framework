package livelib.pages;

import livelib.decorator.WebDriverDecorator;

public class MainPage extends BaseLiveLibPage {

    public MainPage(WebDriverDecorator webDriverDecorator) {
        super(webDriverDecorator);
    }
}
